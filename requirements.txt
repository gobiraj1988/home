# --- Core MCP framework ---
mcp>=1.2.0

# --- Config / env ---
python-dotenv>=1.0.0
pydantic>=2.6.0

# --- RAG: document ingestion + retrieval ---
pandas>=2.1.0            # CSV / tabular research data
pypdf>=4.0.0             # PDF research docs
python-docx>=1.1.0       # Word research docs
rank-bm25>=0.2.2         # lexical retrieval (no GPU / no API key)
numpy>=1.26.0

# --- Optional semantic embeddings (uncomment to enable) ---
# sentence-transformers>=2.6.0

# --- MT5 live data (Windows only) ---
MetaTrader5>=5.0.45 ; platform_system == "Windows"

# --- Backtest sharing via Zapier ---
httpx>=0.27.0
